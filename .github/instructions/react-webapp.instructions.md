---
applyTo: "src/**/*.js,src/**/*.jsx,public/**/*.html"
---

When editing files for this project:
- Implement UI with React and JavaScript (not TypeScript).
- Prefer composition over large monolithic components.
- Keep state local unless shared state is necessary.
- Handle loading and error states for async behavior.
- Use accessible HTML elements and labels.
